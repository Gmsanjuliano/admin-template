import React from "react";
import Layout from "../components/template/layout";

const HomePage: React.FC = () => {
  return (
    <Layout
      title="Página Inicial"
      subTitle="Estaos contruindo um admin template"
    ></Layout>
  );
};

export default HomePage;
